import React from "react";
import Head from "next/head";

const Home = () => (
  <div>
    <Head>
      <title>Admin</title>
      <link rel="icon" href="/favicon.ico" />
    </Head>
    <div className="hero primary-border">tenant admin pnael</div>
  </div>
);

export default Home;
